name: flutter_chatsen_irc
description: IRC Client for Chatsen

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  collection: ^1.19.0

dev_dependencies:
  lints: ^5.1.0
